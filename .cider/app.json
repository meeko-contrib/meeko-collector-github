{
  "name": "cider-collector-github",
  "version": "0.0.1",
  "description": "Cider collector for GitHub events",
  "variables": {
    "LISTEN_ADDRESS": {
      "usage": "TCP network address to listen on; format [HOST]:PORT"
    },
    "ACCESS_TOKEN": {
      "usage": "token being used for request authentication",
      "secret": true
    }
  }
}
